#ifndef _filenamehandler
#define _filenamehandler

char* retornar_extensao(char* nomeArquivo);

char* remover_extensao(char* nomeArquivo);

char* adicionar_extensao(char* nomeArquivo, char* extensao);

#endif
